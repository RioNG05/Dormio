// Placeholder file for global state management.
// Bạn có thể cài đặt zustand (`npm install zustand`) và định nghĩa store tại đây.
// Ví dụ:
// import { create } from 'zustand'
// export const useAuthStore = create((set) => ({ ... }))

export {};
